@model AleksaPortfolio.Models.ViewModels.SimpleHeadingModuleViewModel

@if(!string.IsNullOrWhiteSpace(Model.Heading))
{
    <section class="py-5">
        <div class="container px-5 mb-5">
            <div class="text-center mb-5">
                <h1 class="display-5 fw-bolder mb-0"><span class="text-gradient d-inline">@Model.Heading</span></h1>
            </div>
        </div>
    </section>
}